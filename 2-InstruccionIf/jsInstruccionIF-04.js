function mostrar()
{
	let edad

	edad = parseInt(document.getElementById("txtIdEdad").value);

	if (edad <= 17 && edad >=13){
		alert ("Es adolescente");

	}



		
// "|| operador pipe (or) , && operador ampersand (and) , ! operador not";

}//FIN DE LA FUNCIÓN